import React, {Component} from 'react';

import Users from './Users'

const MainApp = (props) => {
    return (
        <>
            <nav>
            </nav>
            <Users/>
        </>
    );
}

export default MainApp